{% extends "base.html.twig" %}

{% block body %}
<h1> {{ program.title}}</h1>
<h2> Saison {{ season.number}}</h2>


{% for episode in episodes %}
<div>
<a href="{{path('program_episode_show', {program: program.id, season:season.id, episode: episode.id}) }}"><h3>Episode {{episode. number}} / {{episode.title}}</h3></a>
<p> Synopsis: {{ episode.synopsis }}</p>
</div>
{% endfor %}


<a href="{{ path('program_show', {id: program.id}) }}">

		Retour à la liste des saisons

	</a>
{% endblock %}