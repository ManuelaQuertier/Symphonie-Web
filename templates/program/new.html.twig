{% extends "base.html.twig" %}


{% block body %}
<div class="container mt-5">
<h2>Ajouter une série</h2>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button class="btn btn-light" type="submit">Add Program</button>

    {{ form_end(form) }}
</div>

{% endblock %}